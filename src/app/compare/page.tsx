import { CompareWorkspace } from '@/components/CompareWorkspace';
import { LANGUAGES } from '@/lib/languages';

export default function ComparePage() {
  return <CompareWorkspace languages={LANGUAGES} />;
}

